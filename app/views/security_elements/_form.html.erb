<%= form_for(security_element) do |f| %>
  <% if security_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(security_element.errors.count, "error") %> prohibited this security_element from being saved:</h2>

      <ul>
      <% security_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :reglamentario %>
    <%= f.text_field :reglamentario %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
